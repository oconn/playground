/// <reference path="../../../typings/tsd.d.ts" />

const repositoriesReducer: Object = (state: Object = {}, action: any) => {
    switch (action.type) {

    default:
       return state;
    }
};

export default repositoriesReducer;
